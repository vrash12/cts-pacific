# Domain modules

Each module owns its domain types, schemas, queries, commands, and application-level rules. UI components may call a module boundary, but business logic should not be embedded in large React components.

Planned domains: services, projects, leads, products, orders, payments, and chatbot.

