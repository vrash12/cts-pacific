# Administration route

The `/admin` implementation is deferred to the authentication and administration milestone. Authorization will be enforced on the server and in Supabase RLS policies; hidden navigation is never treated as access control.

