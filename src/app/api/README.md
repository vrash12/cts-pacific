# Route handlers

Route handlers will be added per feature. Every externally supplied payload must be validated on the server, rate limited where appropriate, and processed without trusting client-supplied pricing or authorization claims.

