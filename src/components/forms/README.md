# Forms

Project intake and contact form compositions will live here. Shared controls belong in `src/components/ui`; validation schemas belong in `src/schemas`; submission logic belongs in the relevant domain module.

