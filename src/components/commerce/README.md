# Commerce components

This directory is intentionally dormant while `NEXT_PUBLIC_ECOMMERCE_ENABLED=false`. Product, cart, checkout, and purchase controls must not be exposed until the future commerce milestone is enabled and validated.

